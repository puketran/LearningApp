# ── Azure OpenAI ──────────────────────────────────────────────────────────────
ENDPOINT_URL=https://your-resource.openai.azure.com/
DEPLOYMENT_NAME=your_deployment_name
AZURE_OPENAI_API_KEY=your_openai_api_key_here
API_VERSION=2024-02-01

# ── Azure Speech (TTS) ────────────────────────────────────────────────────────
AZURE_SPEECH_KEY=your_azure_speech_key_here
AZURE_SPEECH_REGION=eastus

# ── Data storage ─────────────────────────────────────────────────────────────
# All user data (books, audios, images, recordings) is stored under this folder.
# Accepts an absolute path or a path relative to the project root.
DATA_DIR=./data

# ── Server ────────────────────────────────────────────────────────────────────
PORT=5000
FLASK_DEBUG=false

# ── Basic Auth ────────────────────────────────────────────────────────────────
# Protects every page/API with a browser login prompt.
# Leave both blank (or omit) to disable auth (e.g. local development).
APP_AUTH_USER=your_username
APP_AUTH_PASS=your_password
